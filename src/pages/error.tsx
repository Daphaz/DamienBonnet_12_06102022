/**
 * This is the Error page
 * there is no styling of this page
 * because we need wait the mockup.
 *
 *
 * ```tsx
 * export const Error = (): JSX.Element => {
 * return <div>Error - 404</div>
 * }
 * ```
 */

export const Error = (): JSX.Element => {
  return <div>Error - 404</div>;
};
